Verified classroom research is stored here automatically.

After the Fact Checker, the Research Agent calls `store_knowledge`.
Each file is one topic at one education level, with YAML metadata
and `## Claim` sections so later RAG retrieval can reuse it.

Time-sensitive and unverified work is not written here.
